# Simple Calculator

A basic browser calculator built with vanilla JavaScript, HTML, and CSS —
supports digit entry, basic operators, delete, and all-clear.

## Run

Just open `index.html` in a browser — no build step or dependencies.
